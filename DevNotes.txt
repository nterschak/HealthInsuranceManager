Creating the dotnet API
dotnet new sln
dotnet new webapi -controllers -n API
dotnet sln add API

Add Entity Framework to the project
-Use nuget gallery to add package "Microsoft.EntityFrameworkCore.Sqlite"
-Use nuget gallery to add package "Microsoft.EntityFrameworkCore.Design" (for adding migrations)
-Use nuget gallery to add package "Microsoft.EntityFrameworkCore.Tools"

Configure C# compile to not use nullable reference types
In API.csproj:
<Nullable>disable</Nullable>

Set up source control
On Github.com, create a new repository
Use "git init" to initialize local git repository
Use "git remote add origin" to link local repository to github repository
Use "git branch -M main" to use main branch instead of master if needed

Create the angular application
In the solution folder, execute "ng new client" on the command line

Add ngx-bootstrap to the angular application
execute "ng add ngx-bootstrap" in the terminal